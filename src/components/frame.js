import * as React from 'react'

const Frame = ({ children }) => {
  return (
    <div>
      <p>This would be a picture, but alas, it is not.</p>
      {children}
    </div>
  )
}

export default Frame
